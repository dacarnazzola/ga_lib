# module source code files will be in modules/ folder
add_subdirectory(modules)

# test program source code will be in tests/ folder
add_subdirectory(tests)

# main program will be in mains/ folder
add_subdirectory(mains)
